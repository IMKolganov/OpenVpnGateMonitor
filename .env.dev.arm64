# ASP.NET environment (Development / Production)
ASPNETCORE_ENVIRONMENT=Development
TARGETARCH=arm64
BUILD_CONFIGURATION=Debug

# Database connection string
DB_CONNECTION_STRING_DATAGATE=Host=postgres_backend;Port=5432;Database=datagate_backend;Username=backend_user;Password=securepassword_backend

# JWT secret
JWT_SECRET=super-secret-key-should-be-at-least-16-characters
Jwt__RefreshPepper=your-refresh-pepper

# Frontend -> Backend URL
BACKEND_URL=http://backend:5581
VITE_GOOGLE_CLIENT_ID=your-google-cliend-id

#Google auth
GoogleAuth__ClientId=your-google-cliend-id
GoogleAuth__DesktopClientId=your-desktop-google-cliend-id
GoogleAuth__DesktopClientSecret=your-desktop-google-secret
GoogleAuth__IosClientId=your-google-ios-cliend-id

# Public web origin for TV QR /tv/link (no trailing slash)
Auth__PublicWebBaseUrl=http://localhost:5582
Auth__TvLoginSessionMinutes=5

# Xray (docker-compose.yml): VLESS not on host by default — nginx → xray:443. Manager host port default 15012.
# docker-compose-local.yml defaults: VLESS 30443→443, manager 15012→5010.
# XRAY_MANAGER_HOST_PORT=15012
# XRAY_VLESS_HOST_PORT=30443

# Elasticsearch config
ELASTIC_URI=http://elasticsearch:9200
ELASTIC_USERNAME=elastic
ELASTIC_PASSWORD=your-password
ELASTIC_INDEX_FORMAT=OpenVPNGateMonitor-dev-logs-{0:yyyy.MM.dd}

# Email sender config (smtp|resend)
EmailSender__Provider=smtp
EmailSender__FromEmail=noreply@datagateapp.com
EmailSender__FromName=DataGate
EmailSender__ResendApiKey=
EmailSender__Host=smtp.example.com
EmailSender__Port=587
EmailSender__UseSsl=true
EmailSender__Username=smtp_user
EmailSender__Password=smtp_password